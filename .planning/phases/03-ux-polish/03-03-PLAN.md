---
phase: 03-ux-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - electron/main.js
  - electron/preload.js
  - src/App.jsx
autonomous: true
requirements:
  - UX-04

must_haves:
  truths:
    - "Pressing Space globally triggers push-to-talk even when ORION window is not focused"
    - "Each Space press toggles mic mute state (muted → unmuted, or unmuted → muted)"
    - "The global shortcut is unregistered when the app quits (no orphaned shortcut)"
    - "window.api.onPttToggle is available in the renderer after preload loads"
    - "The PTT effect uses the same toggleMute logic as the existing mute button"
  artifacts:
    - path: "electron/main.js"
      provides: "globalShortcut.register('Space') in app.whenReady + unregisterAll in will-quit"
      contains: "globalShortcut"
    - path: "electron/preload.js"
      provides: "onPttToggle exposed via contextBridge"
      contains: "onPttToggle"
    - path: "src/App.jsx"
      provides: "useEffect that calls window.api.onPttToggle with toggleMute callback"
      contains: "onPttToggle"
  key_links:
    - from: "electron/main.js globalShortcut"
      to: "electron/preload.js ipcRenderer.on('ptt-toggle')"
      via: "mainWindow.webContents.send('ptt-toggle')"
      pattern: "ptt-toggle"
    - from: "electron/preload.js onPttToggle"
      to: "src/App.jsx useEffect"
      via: "window.api.onPttToggle(callback)"
      pattern: "window\\.api\\??\\.onPttToggle"
    - from: "src/App.jsx onPttToggle callback"
      to: "toggleMute function"
      via: "direct call"
      pattern: "toggleMute"
---

<objective>
Wire a global Space shortcut through Electron's globalShortcut → IPC → preload → App.jsx toggleMute.

Purpose: Users can push-to-talk hands-free even when ORION is in the background — activating the mic with a single keypress without switching windows.
Output: Three files edited; Space globally toggles mic mute.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ux-polish/03-CONTEXT.md

<interfaces>
<!-- Existing IPC pattern in electron/main.js (lines 105-121) -->
ipcMain.on('window-minimize', () => { if (mainWindow) mainWindow.minimize(); });
ipcMain.on('window-maximize', () => { ... });
ipcMain.on('window-close', () => { if (mainWindow) mainWindow.close(); });

<!-- Existing preload.js contextBridge (full file) -->
contextBridge.exposeInMainWorld('api', {
  minimize: () => ipcRenderer.send('window-minimize'),
  maximize: () => ipcRenderer.send('window-maximize'),
  close: () => ipcRenderer.send('window-close'),
  openExternal: (url) => shell.openExternal(url),
});

<!-- Existing App.jsx mute toggle (lines 1087-1097) -->
const toggleMute = () => {
    if (!isConnected) return;
    if (isMuted) {
        socket.emit('resume_audio');
        setIsMuted(false);
    } else {
        socket.emit('pause_audio');
        setIsMuted(true);
    }
};

<!-- app.on('will-quit') location: electron/main.js line 193 -->
app.on('will-quit', () => {
    console.log('App closing... Killing Python backend.');
    if (pythonProcess) { ... }
});
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Register global Space shortcut in main.js and expose onPttToggle in preload.js</name>
  <files>electron/main.js, electron/preload.js</files>

  <read_first>
    - electron/main.js lines 1-5 (destructure line)
    - electron/main.js lines 104-139 (app.whenReady block — find end of ipcMain handlers)
    - electron/main.js lines 193-210 (will-quit handler)
    - electron/preload.js (full file — 8 lines)
  </read_first>

  <action>
**electron/main.js — three changes:**

Change 1: Update the destructure on line 2 to add `globalShortcut`:
```js
// Before:
const { app, BrowserWindow, ipcMain } = electron;
// After:
const { app, BrowserWindow, ipcMain, globalShortcut } = electron;
```

Change 2: In `app.whenReady()` (line 104), after the three `ipcMain.on(...)` handlers (after line 121, before the `checkBackendPort` call on line 123), insert:
```js
    globalShortcut.register('Space', () => {
        if (mainWindow) mainWindow.webContents.send('ptt-toggle');
    });
```

Change 3: In `app.on('will-quit', ...)` (line 193), add `globalShortcut.unregisterAll();` as the FIRST line inside the handler, before the pythonProcess kill block:
```js
app.on('will-quit', () => {
    globalShortcut.unregisterAll();
    console.log('App closing... Killing Python backend.');
    if (pythonProcess) {
        ...
    }
});
```

**electron/preload.js — one change:**

Add `onPttToggle` as a new property in the existing `contextBridge.exposeInMainWorld('api', {...})` object. Add it after `openExternal`:
```js
contextBridge.exposeInMainWorld('api', {
  minimize: () => ipcRenderer.send('window-minimize'),
  maximize: () => ipcRenderer.send('window-maximize'),
  close: () => ipcRenderer.send('window-close'),
  openExternal: (url) => shell.openExternal(url),
  onPttToggle: (callback) => ipcRenderer.on('ptt-toggle', (_event) => callback()),
});
```

Notes:
- `globalShortcut.register` returns `true` on success — no error handling needed for MVP
- `_event` is intentionally unused (IPC event object not needed by the callback)
- `ipcRenderer.on` (not `ipcRenderer.once`) so every Space press fires the callback, not just the first
  </action>

  <verify>
    <automated>grep -n "globalShortcut\|ptt-toggle\|onPttToggle" /home/falves/Documentos/gits/orion/electron/main.js /home/falves/Documentos/gits/orion/electron/preload.js</automated>
  </verify>

  <acceptance_criteria>
    - `grep "globalShortcut" electron/main.js` returns at least 3 matches (destructure, register, unregisterAll)
    - `grep "ptt-toggle" electron/main.js` returns exactly 1 match (webContents.send)
    - `grep "globalShortcut.unregisterAll" electron/main.js` returns 1 match inside the will-quit handler
    - `grep "onPttToggle" electron/preload.js` returns 1 match
    - `grep "ptt-toggle" electron/preload.js` returns 1 match inside ipcRenderer.on
  </acceptance_criteria>

  <done>main.js registers Space shortcut on ready and unregisters on quit; preload.js exposes onPttToggle to renderer via contextBridge.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Register PTT callback in App.jsx using existing toggleMute</name>
  <files>src/App.jsx</files>

  <read_first>
    - src/App.jsx lines 315-340 (socket setup useEffect — find the block to insert after)
    - src/App.jsx lines 1087-1097 (toggleMute function — confirm exact name)
    - src/App.jsx lines 44-50 (isConnected state)
  </read_first>

  <action>
In src/App.jsx, find the socket setup useEffect (around line 318). After that useEffect's closing `}, []);` line, insert a new useEffect:

```js
useEffect(() => {
    if (window.api?.onPttToggle) {
        window.api.onPttToggle(() => {
            toggleMute();
        });
    }
}, []);
```

Important notes:
- Use `toggleMute()` directly — it already handles the `if (!isConnected) return` guard and the socket.emit calls. Do NOT duplicate the mute logic.
- `window.api?.onPttToggle` optional chaining guards against running in browser dev mode where `window.api` is undefined
- The empty dependency array `[]` is correct — the callback is registered once on mount; `toggleMute` closes over the latest state via `setIsMuted` functional updates (the existing toggleMute uses direct state read `if (isMuted)`, which means this useEffect MUST be placed after `toggleMute` is defined — confirm `toggleMute` is defined before this useEffect's location, or move the useEffect to after toggleMute's definition around line 1097)
- If the socket setup useEffect location (line ~318) is BEFORE `toggleMute` is defined (~line 1087), place this new useEffect AFTER `toggleMute`'s definition instead (after line 1097), not near line 318.
  </action>

  <verify>
    <automated>grep -n "onPttToggle\|toggleMute" /home/falves/Documentos/gits/orion/src/App.jsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep "onPttToggle" src/App.jsx` returns at least 1 match
    - `grep "window.api?.onPttToggle\|window\.api\?\.onPttToggle" src/App.jsx` returns 1 match
    - The useEffect containing `onPttToggle` appears AFTER the `toggleMute` function definition in the file
    - The callback body calls `toggleMute()` (not a reimplementation of mute logic)
    - The useEffect has an empty dependency array `}, []);`
  </acceptance_criteria>

  <done>App.jsx registers the PTT callback on mount; each Space press calls toggleMute(), toggling mic state and emitting the correct socket event.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS keyboard hook → Electron main | globalShortcut fires for all Space keypresses system-wide |
| Electron main → renderer IPC | ptt-toggle IPC message crosses process boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Elevation of Privilege | globalShortcut Space | accept | Only toggles mic mute — no privileged OS operation; CONTEXT.md explicitly accepts Space without modifier for v1 |
| T-03-03-02 | Denial | Space captured while typing | accept | CONTEXT.md deferred "modifier key" variant to v2; user understands this tradeoff |
| T-03-03-03 | Spoofing | ptt-toggle IPC | accept | IPC is local process-to-process; no external origin can send this message |
| T-03-03-04 | Denial | globalShortcut not unregistered on crash | mitigate | `globalShortcut.unregisterAll()` in will-quit handler covers normal and forced quit paths |
</threat_model>

<verification>
After both tasks complete:
1. `npm run dev` — app starts, no console errors
2. With ORION window NOT focused, press Space — mic toggles (check mute button visual state in UI)
3. Press Space again — mic toggles back
4. Close the app — Space key returns to normal OS behavior (shortcut unregistered)
5. Existing mute button in UI still works independently
</verification>

<success_criteria>
- Pressing Space globally fires toggleMute() in App.jsx
- Shortcut is cleaned up on app quit
- window.api.onPttToggle is accessible from renderer (no contextBridge errors)
- Existing mute button behavior unchanged
- App compiles and starts without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-ux-polish/03-03-SUMMARY.md`
</output>
